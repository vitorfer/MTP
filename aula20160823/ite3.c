#include <stdio.h>

int main () {
    int i, cont = 0;
    char frase;
    printf("Digite uma frase: ");
    scanf("%s",&frase);
    for(j = 0; j != '\0'; j++)
    {

    for(i = 0; i <'\0'; i++)
    {
        cont++;
    }
    printf("O numero de caracteres e: %d", cont);
    return 0;

}

