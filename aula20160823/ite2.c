#include <stdio.h>

int main () {
    int numero_base, numero_potencia = 0, i, resultado = 1;
    printf("Entre com o valor de dois numeros inteiros para a base e a potencia: ");
    scanf("%d%d",&numero_base,&numero_potencia);
    for(i = 0; i < numero_p

        resultado = resultado * numero_base;
    }
    printf("A resposta e: %d", resultado);
    return 0;

}

